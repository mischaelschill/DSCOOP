note
	description: "{CLI_COMMAND} is a structure describing a CLI command."
	author: "Mischael Schill"
	date: "$Date$"
	revision: "$Revision$"

deferred class
	CLI_COMMAND

feature
	name: ESTRING_8
		deferred
		end

	call (a_arguments: LIST[ESTRING_8])
		deferred
		end

end
